import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx' // Le fichier avec le code du jeu que je t'ai donné !
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
